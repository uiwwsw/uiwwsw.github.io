# Rebuild Prep

This repository has been reset to prepare for a fresh Next.js/TypeScript/Tailwind/MDX blog build. All previous Jekyll pages, posts, and assets were removed to eliminate AI-like content and prototype layouts while keeping the Git history intact. Only `.git` metadata and `.gitignore` remain so the next steps can scaffold the new stack cleanly.

## Next steps
- Initialize the new app scaffold (Next.js App Router, TypeScript, Tailwind, MDX).
- Reintroduce content with human, project-grounded case studies and posts.
- Rebuild layouts, navigation, and design system from scratch.
