# 프로젝트 분석 및 인수인계 문서

분석 기준 시점: 2026-03-17 (Asia/Seoul)

## 1. 이 프로젝트가 정확히 무엇인가

이 프로젝트는 일반적인 포트폴리오 사이트도 아니고, 완전한 블로그 프런트엔드도 아닙니다.

정확히는 `uiwwsw`의 Velog 글을 데이터 소스로 삼아, 글 속 문장과 코드 조각을 3D 공간에 흩뿌려 탐색하게 만드는 개인 브랜딩형 인터랙티브 사이트입니다. 사용자는 문장 우주를 비행하다가 특정 문장을 선택하고, 그 문장이 속한 글의 주변 문맥 일부를 본 뒤 원문으로 이동합니다.

한 줄 정의:

- 정체성: 생각과 코드의 조각을 공간적으로 탐색하는 개인 아카이브
- 역할 1: 작성자의 분위기와 세계관을 전달하는 브랜딩 랜딩 페이지
- 역할 2: Velog 글을 탐색하게 만드는 발견형 인터페이스
- 아직 아닌 것: 완성형 자체 블로그, 검색 가능한 지식베이스, 정보 구조가 안정된 콘텐츠 플랫폼

## 2. 현재 상태 요약

현재 커밋된 데이터 기준으로 확인한 수치:

- `src/data/velog-context.json` 기준 글 수: `37`
- `src/data/velog-words.json` 기준 클라우드 표시 조각 수: `800`
- 전체 문맥 데이터 구성: `텍스트 708`, `코드 53`, `이미지 0`
- 클라우드 데이터 구성: `텍스트 749`, `코드 51`
- `./node_modules/.bin/vite build` 기준 로컬 프로덕션 빌드 성공 확인

현재 UX를 실제 코드 기준으로 요약하면:

- 첫 화면: 검은 우주 배경 위에 3D 문장 클라우드
- 상호작용: 스크롤, 드래그, 터치 기반 비행/회전
- 선택: 문장을 누르면 흰색 아티클 오버레이로 전환
- 전환 후: 선택 문장 중심의 주변 문맥 5개 내외 표시
- 최종 CTA: Velog 원문 열기

즉, 이 사이트는 "글을 직접 읽는 곳"보다는 "글의 세계관 안으로 진입시키는 관문"에 더 가깝습니다.

## 3. 기술 스택과 구조

핵심 스택:

- React 18
- Vite 5
- Three.js
- `@react-three/fiber`
- `@react-three/drei`
- `@react-three/postprocessing`
- Node 스크립트 + `undici` + `cheerio`
- GitHub Actions + GitHub Pages

핵심 파일 맵:

- `package.json`: 실행 스크립트와 의존성
- `vite.config.js`: Vite 루트, 정적 파일, 빌드 출력 설정
- `src/main.jsx`: React 진입점
- `src/App.jsx`: 전체 캔버스, 오버레이, 상세 보기
- `src/components/WordCloud.jsx`: 3D 인터랙션 핵심 로직 대부분
- `src/index.css`: 전체 스타일
- `scripts/fetch-velog.js`: Velog 데이터를 가져와 JSON으로 변환
- `scripts/update-cache-version.js`: 서비스워커 버전 문자열을 소스 파일에 직접 주입
- `public/sw.js`: 캐싱 로직
- `.github/workflows/deploy.yml`: 배포 자동화
- `.github/workflows/monthly-update.yml`: 월간 데이터 갱신 자동화

## 4. 런타임 동작 구조

### 화면 흐름

1. `src/main.jsx`가 `App`을 마운트합니다.
2. `src/App.jsx`가 전체 화면 `Canvas`를 렌더링합니다.
3. `App`은 지연 로딩으로 다음을 불러옵니다.
   - `WordCloud`
   - `velog-context.json`
4. `WordCloud`는 `velog-words.json`을 다시 지연 로딩하고, 브라우저에서 3D 위치를 생성합니다.
5. 사용자가 문장을 선택하면 메타데이터가 `App`으로 전달됩니다.
6. `App`은 해당 문장이 속한 글의 전체 문맥에서 앞뒤 문장을 잘라 5개 내외의 문맥 뷰를 만듭니다.
7. 상세 보기는 WebGL 내부가 아니라 HTML 오버레이로 뜹니다.

### 시각 구조

- 배경: 검은 캔버스 + Bloom + Vignette
- 전면 오버레이: 최소한의 타이틀과 서브카피
- 상세 오버레이: 흰 바탕의 읽기 화면

이 검정 우주와 흰 읽기 화면의 대비는 현재 코드베이스에서 가장 좋은 디자인 결정 중 하나입니다.

## 5. 데이터 파이프라인

이 프로젝트의 CMS는 사실상 Velog입니다.

`scripts/fetch-velog.js`의 흐름:

1. Velog GraphQL로 `uiwwsw`의 전체 글 목록을 가져옵니다.
2. 각 글의 HTML을 직접 가져옵니다.
3. 페이지 안의 `window.__APOLLO_STATE__`를 파싱합니다.
4. 거기서 markdown body를 꺼냅니다.
5. markdown을 코드 블록과 일반 텍스트로 나눕니다.
6. 일반 텍스트는 문장 단위로 분해합니다.
7. 최종적으로 두 파일을 만듭니다.
   - `src/data/velog-context.json`: 글 전체 문맥용
   - `src/data/velog-words.json`: 클라우드 표시용 샘플

현재 중요한 특성:

- 최대 클라우드 문장 수는 `800`
- 클라우드용 데이터는 저장 시 랜덤 셔플됩니다
- 3D 위치도 브라우저에서 다시 랜덤 생성됩니다

즉, 같은 사이트라도 새로고침/재빌드에 따라 보여지는 문장 조합과 위치가 둘 다 달라질 수 있습니다.

## 6. 실행과 배포 방식

### 로컬 명령어

- 설치: `npm install`
- 개발 서버: `npm run dev`
- 데이터만 새로 받기: `npm run fetch-data`
- 전체 빌드: `npm run build`
- 프리뷰: `npm run preview`

### 중요한 주의점

`npm run build`는 단순 프런트엔드 빌드가 아닙니다.

실제 순서:

1. `scripts/update-cache-version.js` 실행
2. `scripts/fetch-velog.js` 실행
3. `vite build` 실행

즉 일반적인 빌드 한 번에도:

- 외부 네트워크와 Velog 응답에 의존하고
- 추적 중인 소스 파일이 수정되고
- UI만 확인하려는 상황에도 데이터와 캐시 버전이 흔들릴 수 있습니다

UI 작업만 안전하게 확인할 때는 아래가 더 적절합니다.

- `npm run dev`
- `./node_modules/.bin/vite build`

### 배포 방식

- `main` 브랜치 push 시 GitHub Pages 배포
- 매월 스케줄 배포도 존재
- 별도의 월간 데이터 갱신 워크플로우도 존재

## 7. 현재 프로젝트의 강점

- 컨셉이 분명하고 흔한 포트폴리오 같지 않습니다.
- Velog 글을 실제 데이터 소스로 쓰기 때문에 감성만 있는 사이트가 아니라 작성물과 연결된 실체가 있습니다.
- 첫 화면의 분위기는 강합니다. "이 사람 사이트다"라는 인상이 남습니다.
- 3D 캔버스와 읽기 오버레이의 대비가 명확합니다.
- 대용량 JSON과 핵심 컴포넌트를 lazy load하는 방향은 적절합니다.
- 배포와 데이터 갱신의 자동화 기초가 이미 있습니다.

## 8. 면밀히 봤을 때 확인된 문제점

아래는 이후 같은 개발을 이어갈 때 반드시 알고 있어야 할 핵심 리스크입니다.

### 8.1 `WordCloud.jsx`가 사실상 시스템 전체입니다

`src/components/WordCloud.jsx`는 `1102`줄짜리 거대 파일이고, 안에 아래가 모두 섞여 있습니다.

- 레이아웃 생성
- 입력 처리
- 카메라 이동
- 인트로 연출
- 제스처 처리
- hover/click 판정
- 전역 상태 비슷한 복원 로직
- 렌더링

이 상태에서는 UI/UX를 크게 바꾸려 할수록 사고 확률이 올라갑니다. 지금 가장 먼저 손봐야 할 기술 부채입니다.

### 8.2 React 바깥의 모듈 전역 상태 의존성이 큽니다

`WordCloud.jsx`에는 다음 전역 상태가 있습니다.

- `globalIsIntro`
- `globalUserPos`
- `globalUserQuat`

이 방식이 당장 동작은 하지만, 이후 리팩터링이나 상태 추적을 어렵게 만듭니다. 특히 remount 시 어떤 값이 살아남는지 코드 문맥을 읽지 않으면 이해하기 어렵습니다.

### 8.3 데이터도 랜덤, 위치도 랜덤입니다

랜덤성이 두 번 들어갑니다.

- `scripts/fetch-velog.js`에서 클라우드용 문장을 랜덤 셔플 후 일부 선택
- `src/components/WordCloud.jsx`에서 각 문장의 3D 위치를 랜덤 생성

이 구조의 결과:

- 빌드마다 어떤 문장이 보일지 달라질 수 있음
- 새로고침마다 위치가 달라짐
- 사용자가 공간을 기억하기 어려움
- 특정 문장/군집을 다시 찾거나 공유하기 어려움

지금은 "우연성 기반 체험"에는 맞지만, 장기적으로는 "아카이브"라는 성격과 충돌합니다.

### 8.4 상세 화면은 아직 읽기 경험이 아니라 미리보기 수준입니다

`src/App.jsx`는 선택된 문장 주변 문맥 몇 개만 보여주고, 결국 Velog 원문으로 보내는 구조입니다.

현재 빠진 것:

- 글 날짜
- 태그
- 읽는 데 걸리는 시간
- 같은 글 안의 다른 포인트 이동
- 전체 markdown 렌더링
- 실제 이미지
- 코드 하이라이팅

즉 지금의 상세 화면은 분위기는 괜찮지만, 완성도 있는 읽기 면에서는 아직 얕습니다.

### 8.5 월간 데이터 갱신 워크플로우가 불완전합니다

`.github/workflows/monthly-update.yml`은 현재 `src/data/velog-words.json`만 커밋 대상으로 보고 있습니다.

그런데 실제 스크립트는 두 파일을 모두 만듭니다.

- `src/data/velog-words.json`
- `src/data/velog-context.json`

그래서 저장소 안에서는 클라우드 데이터와 문맥 데이터의 최신성이 어긋날 가능성이 있습니다. 배포 시점에는 다시 fetch가 돌기 때문에 운영에서는 티가 덜 날 수 있어도, 저장소 자체는 항상 최신 상태라고 보기 어렵습니다.

### 8.6 서비스워커 캐시 조건문에 논리 우선순위 문제가 있습니다

`public/sw.js`의 캐싱 조건문은 `&&`와 `||`를 괄호 없이 섞어 쓰고 있습니다.

의도는 "data/API는 제외하고 document/script/style/image만 캐시"에 가깝지만, 현재 표현은 연산자 우선순위 때문에 그 의도가 분명하게 보장되지 않습니다. 이 부분은 명시적인 괄호나 별도 helper 함수로 다시 써야 합니다.

### 8.7 빌드가 추적 파일을 직접 수정합니다

`scripts/update-cache-version.js`는 아래 파일을 직접 수정합니다.

- `public/sw.js`
- `src/index.html`

즉 빌드가 순수 함수가 아닙니다. 이 구조는 로컬 작업, CI, 재현성 측면에서 모두 좋지 않습니다.

### 8.8 반쯤 남아 있는 코드가 보입니다

예시:

- `src/App.jsx`에 nav/stats 섹션이 주석으로 남아 있음
- `SentenceWrapper`는 `isDetailMode`를 받지만 실제 부모에서 넘기지 않음
- 상세 모드 opacity 분기에서 `isSelected`를 참조하지만 컴포넌트 내부 정의가 없음
- `contextSentences`를 `WordCloud`로 넘기지만 실제로 사용하지 않음

바로 에러로 터지는 부분만 문제는 아닙니다. 이런 흔적이 쌓이면 이후 개발자가 현재 진짜 동작 경로와 죽은 경로를 구분하는 데 시간이 많이 듭니다.

## 9. 현재 UI/UX 진단

현재 UI는 "좋은 컨셉"은 있지만 "강한 시스템"은 아직 아닙니다.

### 좋은 점

- 첫인상이 강함
- 감성이 분명함
- 검정 우주와 흰 읽기 화면의 대비가 좋음
- 군더더기가 적음

### 아쉬운 점

- 처음 들어온 사용자는 무엇을 해야 하는지 직관적으로 이해하기 어렵습니다
- 메인 카피는 분위기 중심이라 정보 전달력이 약합니다
- 정보 계층이 거의 없습니다
- 상세 화면은 깔끔하지만, 랜딩의 독창성에 비해 다소 평범하게 느껴집니다
- 검색/필터/길찾기 기능이 거의 없습니다
- 모바일에서는 정교한 3D 조작을 요구받는 편이라 피로도가 높습니다

## 10. UI/UX를 "완전 더 멋있게" 만들 방향

이 프로젝트를 더 멋있게 만드는 방법은 단순히 CSS를 예쁘게 바꾸는 것이 아닙니다. 제품의 형태를 더 선명하게 잡아야 합니다.

### 추천 방향: "생각의 우주"에서 "탐색 가능한 Thought Atlas"로

핵심은 이렇습니다.

- 우주/공간 메타포는 유지
- 탐색의 가독성은 높임
- 선택 후 읽기 경험은 깊게 만듦
- 모바일 진입 장벽은 낮춤
- 시각적 스펙터클보다 방향감과 서사를 강화

### 메인 캔버스 개선 방향

추천 요소:

- 짧은 인터랙션 가이드 추가
  - 예: `스크롤해 생각 사이를 비행하고, 문장을 눌러 글의 문맥으로 들어갑니다.`
- 최소한의 HUD 추가
  - 총 글 수
  - 현재 표시 조각 수
  - 현재 hover된 글 제목
- 랜덤 분산 대신 글/주제/연도 단위 군집화
- hover/선택 시 관련 문장끼리 약한 선이나 halo로 연결
- 비주얼 방향 강화
  - 기본 보라 포인트 컬러 의존도 축소
  - 더 의도적인 타이포 조합
  - 단순 black + bloom을 넘는 깊이감 있는 배경
- 비행이 싫은 사용자를 위한 검색 또는 커맨드 팔레트 제공

### 상세 화면 개선 방향

현재의 흰 읽기 화면은 버릴 필요 없습니다. 다만 더 "작가의 화면"처럼 보여야 합니다.

추가 추천:

- 글 제목, 발행일, 태그, 글 위치 표시
- 같은 글 내부의 다른 핵심 문장 이동
- 실제 이미지 렌더링
- 코드 블록 하이라이팅
- 같은 글 또는 비슷한 글의 관련 조각 rail
- `별자리로 돌아가기`와 `Velog에서 원문 보기`를 명확히 분리

### 발견 UX 개선 방향

- 키워드 검색
- 연도/주제/글 타입 필터
- 선택 상태를 URL에 반영
- 미니맵 또는 오비트 맵 제공
- 대표 글이나 대표 주제를 "추천 별자리"처럼 제공

### 모바일 UX 개선 방향

- 제스처 단순화
- 탭 중심 탐색 모드 제공
- 정교한 드래그/관성 조작 의존도 축소
- 모바일에서만 2.5D 또는 guided mode를 두는 것도 고려 가치가 큼

## 11. 이후 동일한 개발을 가능하게 하는 권장 순서

이 프로젝트를 다시 이어서 개발한다면 아래 순서가 가장 안전합니다.

### 1단계: 기반 안정화

- `WordCloud.jsx` 분리
  - 레이아웃 생성
  - 카메라 컨트롤
  - 상호작용 판정
  - 아이템 렌더링
- 죽은 prop/state 정리
- 서비스워커 조건문 수정
- 월간 갱신 워크플로우가 두 JSON 파일을 모두 반영하게 수정
- `build app`과 `refresh content`를 분리

### 2단계: 콘텐츠 모델 강화

수집 단계에서 아래 메타데이터를 같이 저장하도록 확장하는 것이 좋습니다.

- 발행일
- 태그
- 짧은 요약
- 썸네일 또는 이미지 URL
- 예상 읽기 시간
- slug 기반 안정 ID

이 단계가 있어야 이후 UI를 진짜 풍부하게 만들 수 있습니다.

### 3단계: UX 시스템 재설계

- 디자인 토큰 정의
- 메인 히어로와 정보 계층 재설계
- 상세 화면을 고급 editorial surface로 재구성
- 검색/필터/길찾기 도입

### 4단계: 공간 시스템 재설계

- 랜덤 배치에서 결정적 배치로 전환
- 글/주제 중심 클러스터 설계
- seed 기반 위치 생성
- 군집 간 이동 연출 추가

## 12. 이후 개발 시 지켜야 할 원칙

- Velog를 소스 오브 트루스로 유지하되, 모든 프런트엔드 빌드가 실시간 fetch에 묶이지 않게 할 것
- 새로운 기능을 `src/components/WordCloud.jsx`에 계속 쌓지 말 것
- 이 사이트의 본질은 "공간적으로 탐색하는 개인 글 아카이브"라는 점을 유지할 것
- 평범한 블로그 리스트로 퇴화시키지 말 것
- 랜덤 스펙터클보다 기억 가능한 구조를 우선할 것
- UI 개선은 애니메이션 추가보다 방향감과 내러티브 강화를 우선할 것
- 데스크톱과 모바일은 같은 화면을 억지로 줄이지 말고, 별도 경험으로 설계할 것

## 13. 가장 먼저 할 다음 작업 추천

실제로 바로 작업에 들어간다면 추천 순서는 이렇습니다.

1. `WordCloud.jsx`를 기능 단위로 분리하는 리팩터링
2. 워크플로우와 서비스워커의 정확성 문제 수정
3. 데이터 수집 결과에 메타데이터 확장
4. 상세 오버레이를 더 고급스럽게 재설계
5. 랜덤 레이아웃을 군집 기반 결정적 레이아웃으로 교체
6. 검색/필터/미니맵 추가

## 14. 최종 판단

이 프로젝트는 컨셉이 약한 것이 아닙니다. 오히려 컨셉은 이미 충분히 좋습니다.

현재 막히는 지점은 "아이디어 부족"이 아니라 "강한 프로토타입 상태에 머물러 있는 구현 구조"입니다. 그래서 다음 액션은 순서가 중요합니다.

- 먼저 구조를 안정화하고
- 그 다음 콘텐츠 모델을 풍부하게 만들고
- 그 위에서 진짜 UI/UX 재설계를 해야 합니다

그 순서를 지키면 이 프로젝트는 단순히 신기한 실험이 아니라, 아주 인상적인 개인 사이트로 발전할 가능성이 높습니다.
