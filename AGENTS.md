# Repository workflow

- The owner requests that completed code changes be committed and pushed on every implementation task. Run appropriate checks, commit the task's changes, and push to the current remote branch unless the user explicitly asks otherwise. Never include unrelated user changes.
- This is uiwwsw's personal portfolio, deployed from `main` to GitHub Pages. Preserve the existing deployment and monthly Velog refresh workflows.
- The visual direction is an immersive journey from the Moon toward Earth, with one discoverable star per real Velog article. Keep Korean reading content, keyboard/touch access, reduced motion, and a non-WebGL article-list fallback working.
